import { Typography } from '@mui/material';

const SearchPage = () => {
  return (
    <div>
      <Typography sx={{ color: 'primary.main' }}>Search Page</Typography>
    </div>
  );
};

export default SearchPage;
